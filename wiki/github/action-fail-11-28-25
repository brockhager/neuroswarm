
Action Name:
NS-LLM Native Build #23

Latest Notes:

Fixed! The workflow now:

Continues on generative export failure - Uses || echo "Warning..." to continue if GPT-2 export fails
Better error messages - Distinguishes ImportError from other exceptions and suggests compatible versions
Prioritizes critical exports - Embedding model (all-MiniLM-L6-v2) is still required, generative is optional
This allows the build to complete even when there are torch/optimum version compatibility issues with the generative model export. The embedding model export appears to be working fine based on the logs.

