{
  "name": "vp-node",
  "version": "0.1.0",
  "description": "VP Node prototype (deterministic producer core + tests)",
  "type": "module",
  "scripts": {
    "test:unit": "node --test tests/unit/**/*.mjs",
    "test:integration": "node --test tests/integration/**/*.mjs",
    "test:integration:emulator": "node ../scripts/test-with-firestore-emulator.mjs vp-node",
    "test": "npm run test:unit && npm run test:integration"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@google/generative-ai": "^0.21.0",
    "express": "^4.19.2",
    "uuid": "^9.0.0"
  }
}