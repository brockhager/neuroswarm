name: 'Publish Wiki Now (DISABLED DUPLICATE)'

on: []

jobs:
  disabled-job:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled - duplicate workflow
        run: echo "This workflow is a duplicate and intentionally disabled. Use the root-level .github/workflows/publish-wiki-now.yml instead."
